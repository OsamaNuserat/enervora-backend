export enum ThemePreferences {
  DARK = 'dark',
  LIGHT = 'light',
  SYSTEM = 'system',
}

export enum Role {
  User = 'user',
  Admin = 'admin',
  Coach = 'coach',
}

export enum UserStatus {
    ACTIVE = 'Active',
    INACTIVE = 'Inactive',
    SUSPENDED = 'Suspended',
  }
  
